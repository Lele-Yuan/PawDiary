/* components/agent-ui/index.wxss */
.agent-ui {
  width: 750rpx;
  height: 100%;
  position: relative;
  display: flex;
  flex-direction: column;
  overflow:hidden;
  color: var(--text-color);
  background-color: var(--bg-color);
  overflow: hidden;
  touch-action: none; /* 增强禁止滚动效果 */
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
}

.showBotName {
  height: 62px;
}

.hiddenBotName {
  margin-top: 2px;
  margin-bottom: 2px;
}

.nav {
  width: 750rpx;
  padding: 20px 0px 0px 0px;
  display: flex;
  flex-direction: column;
  align-items: center;
  flex-shrink: 0;
}

.navBar {
  width: 100%;
  box-shadow: var(--shadow-card);
}

.tips {
  display: flex;
  align-items: center;
  gap: 12px;
  width: 100%;
  color: var(--text-light);
  font-size: 24rpx;
  height: 32px;
  margin-bottom: 16px;
}
.tips::before,
.tips::after {
  content: '';
  display: inline-block;
  height: 1px;
  transform: scaleY(.5);
  flex-grow: 1;
  border-radius: 2px;
}
.tips::before {
  background-image: linear-gradient(to right, rgba(123, 92, 245, 0), rgba(123, 92, 245, 0.4));
}
.tips::after {
  background-image: linear-gradient(to left, rgba(123, 92, 245, 0), rgba(123, 92, 245, 0.4));
}

.nav-content {
  /* height:62px; */
  display: flex;
  align-items: center;
  justify-content: space-around;
  gap: 12px;
}

.bot-avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: 2px solid rgba(255, 255, 255, 0.8);
  box-shadow: var(--shadow-card);
}

.bot-name {
  font-size: 32rpx;
  font-weight: 500;
  color: var(--text-color);
}

.main {
  background-color: transparent;
  flex-grow: 1;
  position: relative;
}

.share_btn {
  background-color: transparent;
  margin: 0px !important;
  padding: 0rpx !important;
  width: 64rpx !important;
  height: 64rpx;
  border: none !important;
}

.avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 75rpx;
}

.questions {
  margin: 0px 16px 10px 16px;
}

.question_content {
  background-color: var(--primary-bg);
  padding: 16rpx 24rpx;
  border-radius: var(--radius-md);
  display: inline-block;
  font-size: 26rpx;
  font-weight: 400;
  color: var(--primary-dark);
}

.footer {
  width: 100%;
  min-height: 65px;
  max-height: 380px;
  flex-shrink: 0;
  position: relative;
  padding: 0px 16rpx 24px;
  box-sizing: border-box;
}

.footer .file_list {
  display: flex;
  flex-direction: row;
  flex-wrap: nowrap;
  gap: 16rpx;
  overflow-x: scroll;
  padding: 20rpx 0px;
}

.img-box {
  position: absolute;
  top: -100px;
  left: 0px;
  white-space: nowrap;
  width: 100%;
  background-color: var(--bg-color);
}

.img-preview {
  display: inline-block;
  width: 80px;
  height: 80px;
  margin-right: 8px;
  position: relative;
  margin-top: 10px;
}

.img-preview-image {
  width: 80px;
  height: 80px;
  border-radius: var(--radius-sm);
}

.img-preview-loading {
  width: 100%;
  height: 100%;
  position: absolute;
  top: 0px;
  left: 0px;
  background-color: var(--primary-bg);
  border-radius: var(--radius-sm);
}

.img-preview-close {
  width: 16px;
  height: 16px;
  position: absolute;
  right: -8px;
  top: -8px;
}

.input_box {
  display: flex;
  align-items: flex-end;
  flex-direction: row;
  gap: 10rpx;
  position: relative;
  min-height: 40px;
  padding: 0px 0px;
  background-color: transparent;
  transition: all 0.3s;
}

.set_panel_modal {
  position: fixed;
  width: 750rpx;
  height: 100vh;
  left: 0px;
  top: 0px;
  background-color: rgba(0, 0, 0, 0.7);
  z-index: 1000;
}

.set_panel {
  background-color: var(--card-soft-bg);
  position: absolute;
  left: 0px;
  bottom: 0px;
  width: 750rpx;
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
}

.set_panel_funtion {
  display: flex;
  flex-direction: row;
  padding: 10px 16px;
  box-sizing: border-box;
  gap: 10px;
}

.set_panel_cancel {
  height: 60px;
  text-align: center;
  line-height: 40px;
  color: var(--text-color);
  border-top: 1rpx solid var(--primary-bg);
}

.function {
  display: flex;
  width: 150rpx;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 8px;
  background-color: #ffffff;
  color: var(--text-color);
  padding: 12px 6px;
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-card);
}

.icon {
  width: 48rpx;
  height: 48rpx;
}

.text_desc {
  font-weight: 400;
  font-size: 24rpx;
  color: var(--text-secondary);
}

.input_inner_box {
  width: 100%;
  min-height: 54px;
  display: flex;
  align-items: center;
  border: 1rpx solid var(--primary-bg);
  border-radius: var(--radius-md);
  box-sizing: border-box;
  background-color: var(--card-soft-bg);
}

.say_box {
  font-weight: bold;
  justify-content: center;
}

.input {
  padding: 8px;
  color: var(--text-color);
  width: 100%;
  flex: 1;
  max-height: 160px;
  font-size: 28rpx;
}

.say_btn {
  width: 100%;
  height: 100%;
}

.right_btns {
  height: 50px;
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.left_btns {
  height: 50px;
  display: flex;
  align-items: center;
  gap: 10rpx;
}

.set {
  width: 58rpx;
  height: 58rpx;
}

.send-set {
  width: 38rpx;
  height: 38rpx;
}

.system {
  margin-left: 24rpx;
  margin-right: 24rpx;
  border-radius: var(--radius-md);
  margin-top: 12px;
  padding-bottom: 16px;
  box-sizing: border-box;
  position: relative;
}
.avatar-left{
  position:absolute;
  top: 0px;
  left: 0px;
}
.guide_system {
  padding-left: 32rpx;
  padding-right: 32rpx;
  border-radius: var(--radius-md);
  padding-bottom: 16px;
  box-sizing: border-box;
}

.bot_intro_system {
  padding-left: 32rpx;
  padding-right: 32rpx;
  border-radius: var(--radius-md);
  padding-bottom: 16px;
  box-sizing: border-box;
}

.user {
  display: flex;
  justify-content: flex-end;
}

.userContent {
  margin-top: 12px;
  padding-bottom: 16px;
}

.userContent .fileBar {
  display: flex;
  flex-direction: row;
  flex-direction: row-reverse;
  flex-wrap: nowrap;
  padding: 0px 16px;
  overflow-x: scroll;
  max-height: 80px;
  gap: 10px;
  margin-top: 10px;
}

.user .user_content {
  background-color: var(--primary-color);
  color: #ffffff;
  border-radius: var(--radius-md) 0rpx var(--radius-md) var(--radius-md);
  margin-left: 32rpx;
  margin-right: 32rpx;
  padding: 24rpx;
  word-wrap: break-word;
  word-break: break-all;
  font-size: 30rpx;
  position: relative;
  box-shadow: var(--shadow-button);
}

.user .user_content,
.user .user_content text {
  color: #ffffff;
}

.operation-menu {
  position: absolute;
  bottom: -120rpx;
  left: 0;
  background: rgba(0, 0, 0, 1);
  border-radius: 8rpx;
  display: flex;
  padding: 16rpx 24rpx;
  gap: 32rpx;
  z-index: 100;
}

/* 添加三角形指示器 */
.operation-menu::before {
  content: '';
  position: absolute;
  top: -16rpx;      /* 调整三角形位置 */
  left: 30rpx;     /* 调整三角形的水平位置，使其指向文本 */
  border-left: 16rpx solid transparent;
  border-right: 16rpx solid transparent;
  border-bottom: 16rpx solid rgba(0, 0, 0, 0.8);
  width: 0;
  height: 0;
}

.menu-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.menu-item text {
  color: #ffffff;
  font-size: 24rpx;
}

.menu-icon {
  width: 36rpx;
  height: 36rpx;
  filter: brightness(0) invert(1);
}

.feedback_modal {
  position: fixed;
  top: 0px;
  left: 0px;
  width: 750rpx;
  height: 100vh;
  background-color: rgba(0, 0, 0, 0.7);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

.modal {
  background-color: #fff;
  width: 700rpx;
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow-floating);
}

.modal_head {
  height: 40px;
  line-height: 40px;
  padding: 0px 10px;
}

.modal_body {
  padding: 10px;
}

.modal_footer {
  display: flex;
}

.link-box {
  padding: 0px 16px 6px 16px;
}

.tool_box {
  display: flex;
  flex-direction: row;
  box-sizing: border-box;
  gap: 20rpx;
  justify-content: flex-start;
  flex-wrap: nowrap;
  overflow-x: scroll;
  padding: 20rpx 0rpx 0rpx;
}

.webSearchSwitch {
  width: 200rpx;
  height: 30px;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: var(--radius-pill);
  border: 1rpx solid var(--primary-bg);
  font-size: 26rpx;
  background-color: #ffffff;
  color: var(--text-secondary);
}

.feature_enable {
  background-color: var(--primary-bg);
  color: var(--primary-dark);
  border-color: var(--primary-light);
}

.feature_list {
  position: absolute;
  background-color: transparent;
  bottom: calc(100% + 20rpx);
}

.no_feature_list {
  position: absolute;
  background-color: transparent;
  bottom: calc(100%);
}


/* 抽屉遮罩层 */
.drawer-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 998;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.drawer-mask.show {
  opacity: 1;
  visibility: visible;
}

/* 抽屉主体 */
.drawer {
  position: fixed;
  top: 0;
  left: -80%;
  width: 80%;
  height: 100vh;
  background: var(--bg-color);
  z-index: 999;
  transition: all 0.3s ease;
  display: flex;
  flex-direction: column;
}

.drawer.show {
  left: 0;
}

.drawer-header {
  padding: 16rpx 32rpx 16rpx;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.close-icon {
  width: 24px;
  height: 24px;
}

.drawer-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden; /* 防止内容溢出 */
  height: 0; 
  margin-bottom: 20px;
  margin-left:32rpx;
  margin-right:32rpx;
}

.con-icon {
  width: 24px;
  height: 24px;
  margin-left: 16rpx;
  margin-right: 16rpx;
}

.create-new-chat {
  border-radius: var(--radius-pill);
  background: var(--primary-color);
  color: #ffffff;
  cursor: pointer;
  padding: 16rpx 24rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  box-shadow: var(--shadow-button);
  font-weight: 500;
}

.date-title {
  font-size: 26rpx;
  color: var(--text-light);
  font-weight: 400;
}

.con-container {
  margin-top: 12px;
}

.con-block {
  margin-top: 16px;
}

.con-item {
  padding: 12px 8px;
  margin-bottom: 2px;
  border-radius: var(--radius-sm);
  color: var(--text-color);
}

.con-item:active {
  transition: filter 0.4s;
  cursor: pointer;
  background-color: var(--primary-bg);
  filter: brightness(0.98);
}

.selected-con {
  background-color: var(--primary-bg);
  color: var(--primary-dark);
  transition: filter 0.4s;
}

.tool_btn {
  width: 36rpx; 
  height: 36rpx;
  padding: 10rpx;
  border: 1rpx solid var(--primary-bg);
  border-radius: var(--radius-sm);
}

.playing_btn {
  height: 36rpx; 
  padding: 10rpx;
  border: 1rpx solid var(--primary-bg);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  gap: 10rpx;
  position: relative;
}

.speed-switch {
  display: flex;
  align-items: center;
  margin-left: 0rpx;
  padding: 4rpx 12rpx;
  border-radius: var(--radius-pill);
  font-size: 26rpx;
  color: var(--text-color);
  cursor: pointer;
}

.speed-label {
  margin-left: 6rpx;
}

.speed-popup {
  position: absolute;
  bottom: 48rpx;
  right: 0;
  background: #fff;
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-floating);
  z-index: 99;
  padding: 8rpx 0;
  min-width: 80rpx;
}

.speed-option {
  padding: 16rpx 32rpx;
  font-size: 28rpx;
  color: var(--text-color);
  display: flex;
  align-items: center;
  justify-content: flex-start;
}

.speed-option:active {
  background: var(--primary-bg);
}

/* 底部操作菜单弹窗 */
.action-menu-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.4);
  z-index: 1001;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  animation: fadeIn 0.3s ease-out;
}

.action-menu {
  background: #ffffff;
  border-radius: var(--radius-lg) var(--radius-lg) var(--radius-lg) var(--radius-lg);
  overflow: hidden;
  animation: slideUp 0.3s ease-out;
  margin: 0 20rpx 20rpx 20rpx;
  box-shadow: var(--shadow-floating);
}

.action-item {
  padding: 32rpx 0;
  text-align: center;
  font-size: 30rpx;
  color: var(--text-color);
  background: #ffffff;
  border-bottom: 1rpx solid var(--primary-bg);
  position: relative;
}

.action-item:last-child {
  border-bottom: none;
}

.action-item:active {
  background: var(--primary-bg);
}

.cancel-item {
  margin-top: 20rpx;
  border-radius: var(--radius-lg);
  border-bottom: none;
  color: var(--text-secondary);
}

/* 动画效果 */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes slideUp {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}
