.record-page {
  padding-bottom: 140rpx;
}

/* ===== 顶部宠物标签 ===== */
.top-bar {
  display: flex;
  justify-content: flex-start;
  align-items: center;
  padding: 24rpx 32rpx 8rpx;
}

.pet-tag {
  font-size: 26rpx;
  color: #6B2D1A;
  background-color: #F5EDD0;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
}

/* ===== 标签筛选栏 ===== */
.type-tabs {
  display: flex;
  white-space: nowrap;
  padding: 20rpx 28rpx;
  width: auto;
  height: 72rpx;
}

.tab-item {
  display: inline-flex;
  align-items: center;
  padding: 14rpx 36rpx;
  margin-right: 16rpx;
  border-radius: 60rpx;
  font-size: 26rpx;
  color: #6B2D1A;
  background-color: #F0EAD6;
  flex-shrink: 0;
  height: 32rpx;
}

.tab-item.active {
  background: #6B2D1A;
  color: #FFFFFF;
  font-weight: 600;
  box-shadow: 0 4rpx 16rpx rgba(107, 45, 26, 0.25);
}

/* ===== 节标题 ===== */
.section-title-row {
  padding: 32rpx 32rpx 8rpx;
}

.section-title {
  font-size: 40rpx;
  font-weight: 800;
  color: #313333;
  letter-spacing: -1rpx;
}

/* ===== 时间线容器 ===== */
.timeline {
  padding: 16rpx 24rpx 24rpx 8rpx;
}

.timeline-item {
  display: flex;
  min-height: 120rpx;
}

.timeline-left {
  width: 96rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  flex-shrink: 0;
}

.timeline-date {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-top: 16rpx;
  margin-bottom: 8rpx;
}

.timeline-date .date-text {
  font-size: 18rpx;
  color: #999999;
  white-space: nowrap;
  letter-spacing: 0.5rpx;
}

.timeline-date .hour-text {
  font-size: 16rpx;
  color: #BBBBBB;
  white-space: nowrap;
}

.timeline-dot {
  opacity: 0.5;
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  flex-shrink: 0;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.15);
}

.timeline-line {
  width: 3rpx;
  flex: 1;
  background-color: #E8E2D8;
  margin: 8rpx 0;
}

/* ===== 卡片通用 ===== */
.timeline-card {
  flex: 1;
  border-radius: 32rpx;
  padding: 16rpx;
  margin-bottom: 20rpx;
  position: relative;
  overflow: hidden;
}

/* upcoming：白底右上角渐变 */
.card-upcoming {
  background: linear-gradient(135deg, #FFFFFF 60%, rgba(232, 135, 90, 0.08) 100%);
  box-shadow: 0 8rpx 32rpx rgba(149, 73, 49, 0.07);
}

/* completed：灰底 */
.card-completed {
  background-color: #F5F3F1;
}

/* ===== 即将到来标签 ===== */
.upcoming-tag {
  display: inline-block;
  /* background-color: #FE9D7F; */
  color: #FE9D7F;
  font-size: 18rpx;
  font-weight: 700;
  letter-spacing: 1rpx;
  padding: 4rpx 0;
  border-radius: 20rpx;
  margin-bottom: 20rpx;
}

/* ===== 主内容行 ===== */
.card-main {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
}

/* 类型图标圆形 */
.card-icon-circle {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-size: 18rpx;
  flex-shrink: 0;
  margin-right: 16rpx;
}

/* 文字内容区 */
.card-content {
  flex: 1;
  min-width: 0;
}

.card-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #313333;
  margin-bottom: 8rpx;
  line-height: 1.3;
}

.card-subtitle {
  font-size: 24rpx;
  display: block;
}

.subtitle-upcoming {
  color: #5E5F5D;
}

.subtitle-completed {
  color: #888888;
}

.card-details {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-top: 8rpx;
}

.detail-item {
  font-size: 24rpx;
  color: #888888;
}

wx-button[size=mini].complete-btn {
  height: 50rpx;
  line-height: 50rpx;
  background-color: #6B2D1A;
  color: #FFFFFF;
  font-size: 24rpx;
  font-weight: 600;
  border-radius: 40rpx;
  text-align: center;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 160rpx;
  padding: 0;
  margin-right: 0;
}

.complete-btn::after {
  border: none;
}

.complete-btn-hover {
  opacity: 0.85;
}

.bell-btn {
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 24rpx;
  flex-shrink: 0;
}

/* ===== 图片预览 ===== */
.card-images {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 16rpx;
}

.thumb-img {
  width: 120rpx;
  height: 120rpx;
  border-radius: 16rpx;
}

/* ===== 提醒标题行 ===== */
.card-title-row {
  display: flex;
  align-items: center;
  margin-bottom: 8rpx;
}

.card-title-row .card-title {
  margin-bottom: 0;
}

.remind-icon {
  font-size: 28rpx;
  margin-left: 8rpx;
  flex-shrink: 0;
}

/* ===== 提醒进度条 ===== */
.remind-bar {
  display: flex;
  align-items: center;
  margin-top: 16rpx;
}

.remind-progress-track {
  flex: 1;
  height: 12rpx;
  background-color: #E8E2D8;
  border-radius: 6rpx;
  overflow: hidden;
  margin-right: 16rpx;
}

.remind-progress-fill {
  height: 100%;
  border-radius: 6rpx;
  transition: width 0.3s ease;
}

.remind-days-text {
  font-size: 22rpx;
  color: #888888;
  white-space: nowrap;
  flex-shrink: 0;
}

.remind-days-text.overdue {
  color: #C0392B;
  font-weight: 600;
}
